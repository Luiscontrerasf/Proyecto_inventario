from django.db import models

# Create your models here.

#fs = FileSystemStorage(location='/media/photos')

#class producto(models.Model):
 #   name = models.CharField(verbose_name='Nombre' max_length=30)
  #  ubicacion_actual = models.CharField('Tipo',max_length=15)
   # ubicacion_anterior = models.CharField('Tipo',max_length=15)
   # descripcion = models.CharField(max_length=50)
    #cantidad = models.IntegerField()    
    #imagen = models.ImageField(upload_to="productos", null = True)


    #def __str__(self):
    #    return self.name + ' ' + self.tipo_descripcion + ' ' + self.ubicacion_actual + ' ' + self.cantidad